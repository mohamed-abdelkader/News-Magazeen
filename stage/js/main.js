$(document).ready(function () {
  'use strict';

  /* Start preloader
  =====================================*/

    const preloaderDiv = document.querySelector('.loader');
    setTimeout(function () {
      preloaderDiv.className += " fade";
    }, 7000);

  /* End preloader
  =====================================*/
})
